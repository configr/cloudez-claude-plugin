# Configuração de deploy Cloudez.
# Copie para `.cloudez.yaml` na raiz do projeto publicado e ajuste.
# Lembre de adicionar `.cloudez/` ao .gitignore — é onde fica o estado dos
# deploys.
#
# Cada chave dentro de `cloudez:` é um environment. O nome é livre: um projeto
# com um destino só costuma usar `default`.

cloudez:
  staging:
    # O domínio identifica o site.
    domain: staging.meusite.com.br

    # Onde o site vive no servidor, sempre explícito. O padrão da Cloudez é
    # `~/<domain>/www`. O document root do site no painel precisa apontar para
    # `<root>/current` — é o symlink que o deploy troca atomicamente.
    root: ~/staging.meusite.com.br/www

    # Temporário: estes dados vão passar a vir da API da Cloudez, pelo domínio.
    ssh:
      host: srv-12.cloudez.io
      user: deploy
      port: 22

  production:
    domain: meusite.com.br
    root: ~/meusite.com.br/www

    ssh:
      host: srv-12.cloudez.io
      user: deploy
      port: 22
